name: 'CI'

on:
  push:
    branches: [ "main" ]

  pull_request:
    types:
      - opened
      - synchronize

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Run Hello World
      run: echo "Hello, World!"
